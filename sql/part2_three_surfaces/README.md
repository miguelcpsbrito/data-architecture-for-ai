# SQL — Part 2

SQL schemas and queries for Part 2 of the book.
All queries are written in PostgreSQL 15 / Aurora PostgreSQL dialect.
Placeholders are marked with `:your_value` (psql) or `%(param)s` (Python psycopg2).
